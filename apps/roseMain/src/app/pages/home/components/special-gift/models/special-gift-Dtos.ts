export interface HeroSlide {
  title: string;
  subtitle: string;
  btnLabel: string;
  imageUrl: string;
}

export interface CategoryCard {
  tag: string;
  title: string;
  bgGradient: string;
  imagUrl: string;
}